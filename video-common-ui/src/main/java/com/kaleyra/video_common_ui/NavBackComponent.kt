package com.kaleyra.video_common_ui

/**
 * Enum class representing which sdk UI component has requested
 * the launching of the launcher activity
 */
enum class NavBackComponent {
    /**
     * Chat UI component
     */
    CHAT,

    /**
     * Call UI component
     */
    CALL,
}