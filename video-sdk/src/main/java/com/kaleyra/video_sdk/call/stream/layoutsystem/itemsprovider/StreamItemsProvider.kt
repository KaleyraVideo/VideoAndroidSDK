package com.kaleyra.video_sdk.call.stream.layoutsystem.itemsprovider

/**
 * `StreamItemsProvider` is a marker interface that indicates a class is capable of providing
 * a list of `StreamItem`s.
 *
 * This interface serves as a common type for different providers that generate stream items,
 * allowing for polymorphism and flexibility in how stream items are created and managed.
 *
 * Classes implementing this interface are responsible for constructing and returning a list of
 * `StreamItem` objects, which represent
 **/
internal interface StreamItemsProvider